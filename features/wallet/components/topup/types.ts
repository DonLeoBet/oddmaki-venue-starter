/** Shared props for the TopUp button and its adapters. */
export interface TopUpProps {
  /** Optional class names forwarded to the underlying button. */
  className?: string;
  /** Button label. Default: "Top Up". */
  label?: string;
}
